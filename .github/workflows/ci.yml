name: CI

on:
  push:
    branches: [develop]
    paths:
      - "apps/server/**"
      - ".github/workflows/ci.yml"
  # Sin `paths` en pull_request: si la rama protegida exige este check, los PRs
  # solo-web nunca lo reportarían y quedarían stuck. En `push` se mantiene
  # `paths` para no ejecutar el job en cambios ajenos al server en develop.
  pull_request:
    branches: [develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  server-tests:
    name: Server build & tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/server/package-lock.json

      - name: Install server dependencies
        working-directory: apps/server
        run: npm ci

      - name: Generate Prisma client
        working-directory: apps/server
        run: npx prisma generate

      - name: Build (tsc)
        working-directory: apps/server
        run: npm run build

      - name: Test (vitest)
        working-directory: apps/server
        run: npm test
